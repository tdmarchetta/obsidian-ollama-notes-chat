# Screenshots

Drop the following images here. The README's **Screenshots** section links to these exact
filenames, so keep the names as-is (PNG, or GIF where noted).

| File | What to capture |
| --- | --- |
| `chat-sidebar.png` | The right-sidebar chat panel mid-conversation, showing a streamed answer and the context-mode pill in the subheader. |
| `settings.png` | **Settings → Ollama Notes Chat**, showing the Base URL field, model dropdown, and the Retrieval section. |
| `rag-citation.png` | A retrieval-mode answer with `From [[Note#Heading]]` citations rendered as clickable links. |
| `rewrite-diff.png` | The rewrite-in-place diff preview (CM6 decoration) over a selection, before Accept. |
| `history-drawer.png` | The multi-session history drawer open, listing conversations with previews and timestamps. |
| `demo.gif` *(optional)* | A short loop: type a question → tokens stream in → cited note links appear. |

Tips:
- Use a clean vault and a light/dark theme that reads well on GitHub.
- Crop tightly to the panel; keep widths around 800–1200px so they render sharp without bloating the repo.
